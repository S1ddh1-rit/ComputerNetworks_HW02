# Computer Networks HW2 — Ping & Traceroute

## Requirements
- Python 3
- Raw sockets require admin privileges → run with `sudo`
- Documentation:
  - Sphinx (Python package)
  - LaTeX to build PDF docs (MacTeX recommended on macOS)

## Install dependencies
```bash
python3 -m pip install -r requirements.txt
